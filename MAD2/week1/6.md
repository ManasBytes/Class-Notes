# Lecture 6

There is a global variable called **document**. This makes it possible for us to make document manipulation (text, color or clicks or any interactions with the browser)

Selecting an element of html from this.

document.getElementbyId

to change inner html
document.getElementbyId("ID").innerHTML = "new text"
